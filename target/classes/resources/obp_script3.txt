	SELECT M.PARTY_ID, M.REF_NUM_AGENCY
	FROM NGPCOB_SCHEMA.flx_pi_kyc_ver_sum_b M
	WHERE PARTY_ID IN (SELECT INTERNAL_PARTY_ID FROM NGPCOB_SCHEMA.flx_or_applicants WHERE SUBMISSION_ID IN 
	(SELECT A.SUBMISSION_ID FROM NGPCOB_SCHEMA.flx_or_submission_summary_b a  WHERE trunc(a.creation_date) >=  ? 
	and a.initiated_by  = 'OBDX_ANONYMOUS_USER')) AND M.REF_NUM_AGENCY IS NOT NULL GROUP BY M.PARTY_ID, M.REF_NUM_AGENCY ORDER BY 1